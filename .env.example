# Environment variables for ConfHub
# Copy to .env.local for development or configure in your hosting provider for production

# Local Postgres via Docker + Neon HTTP proxy (see docker-compose.yml)
# The Neon HTTP driver inspects the host to decide proxying. Using db.localtest.me enables the local proxy.
DATABASE_URL=postgres://postgres:postgres@db.localtest.me/main

# OAuth (Better Auth)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=